/**
 * This class represents the exit tile where the player reaches to complete the level
 * Static tile (not moving)
 */

class Exit extends Tile {
  constructor(row, col, width, image, symbol) {
    super(row, col, width, image, symbol);
  }
}
